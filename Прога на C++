#include <iostream>

int main()
{
    //задание - проверить, что x число  находится в диапазоне между числами а и b


    //стандартное решение 1 способ
    int a, b, x;
    std::cin >> a >> b >> x;
    if ((a<x) && (b>x))
        std::cout << "YES";
    else
        std::cout << "NO";
